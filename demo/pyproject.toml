[project]
name = "demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=5.2.12",
    "django-book-manager>=0.3.2",
    "django-selectjs>=0.3.7",
    "django-theme-academy>=0.3.13",
    "django-wildewidgets>=1.7.3",
]

[tool.uv.sources]
django-selectjs = { path = "..", editable = true }
# django-wildewidgets = { path = "../../django-wildewidgets", editable = true }
